{% extends 'base.html.twig' %}

{% block title %}Éditer un guide{% endblock %}

{% block body %}
<h1>Éditer un guide</h1>

{{ form_start(form, {'attr': {'enctype': 'multipart/form-data'}}) }}
    {{ form_widget(form) }}
    <button class="btn btn-success mt-3">Enregistrer</button>
{{ form_end(form) }}

<a href="{{ path('guide_index') }}" class="btn btn-secondary mt-3">⬅ Retour à la liste</a>
{% endblock %}
